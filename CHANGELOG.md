# Changelog

## 0.1.0
- add the versioned operation capability manifest with connection-scoped,
  lifecycle-aware provider matrices and fail-closed drift tests
- publish a generated OpenAPI 3.1 contract and typed cross-repository conformance fixtures
- fail CI when advertised routes, Go DTOs, schemas or fixtures drift
- initial starter skeleton for `service-template`
- sample echo service manifest, runtime payloads, verify scripts, packaging scripts, and harness contract
